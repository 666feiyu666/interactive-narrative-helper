# To-do list

- [ ] Extend an authorized itch.io acquisition to record each project's rating value, rating count, and comment count before comment widgets are excluded; retain counts and metadata only, not comment text, and preserve provenance and stable `project_id` values.
- [ ] Design and review the normalization and augmentation method before creating a normalized description corpus, including its source inputs, common structure, derived-field rules, language handling, and human review.
