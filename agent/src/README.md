# Implementation placeholder

No counterfactual design Agent is implemented yet. The first implementation
should be a narrow vertical slice against the schemas in `../schemas/`, not a
generic story-writing application.
