# Prompt conditions

Store versioned, reusable prompt conditions here. Prompts must not embed a
particular case's expected answers. Experiments should reference an exact prompt
version and keep prompts separate from model and sampling configuration.
