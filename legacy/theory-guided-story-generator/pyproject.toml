[project]
name = "fox-and-crow-story-generator"
version = "0.1.0"
description = "Local Jupyter harness for recursive story generation with DeepSeek."
requires-python = ">=3.12,<3.14"
dependencies = [
    "ipykernel>=6.29,<8",
    "jupyterlab>=4.4,<5",
    "langchain-deepseek==1.1.0",
    "python-dotenv>=1.1,<2",
]

[tool.uv]
package = false
