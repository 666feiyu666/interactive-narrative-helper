# Testbeds

Testbeds are runnable research instruments used across multiple experiments.
They do not define the project RQ, and their automated tests do not substitute
for research evaluation.

`fox-and-crow/` is included as an independently versioned Git submodule. Commit
changes inside the submodule repository first, then deliberately update the
parent repository's recorded commit.
