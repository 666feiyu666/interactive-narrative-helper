# Display MVP verification

Run:

```powershell
npm test
```

The focused suite checks demo-data loading, the single request/response shape,
retrieval counts, locally resolved references, trace validation when explicitly
enabled, and the default no-persistence behavior.

These are software checks for the presentation prototype. They do not establish
data quality, research validity, retrieval quality, advice quality, author
utility, or educational effectiveness.
