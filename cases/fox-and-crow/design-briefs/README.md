# Design briefs

Future author-authored variations of the Fox-and-Crow task belong here. A brief
may add a target age, interaction format, values, or constraints, but should
reference the stable case ID and must not overwrite the base case silently.
